## Packages
date-fns | Formatting timestamps for chat and logs
lucide-react | Icons for the interface (already in base but explicit check)
react-markdown | Rendering markdown in chat messages
recharts | Visualizing model statistics if needed

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
