/**
 * Intentional CI break for Run 1 — Validate AI fixer loop.
 * Delete this file or fix the assertion after validation.
 */
import { describe, it, expect } from 'vitest';

describe('ci-break-run1', () => {
  it('intentional fail for AI Fix CI validation', () => {
    expect(1).toBe(2);
  });
});
